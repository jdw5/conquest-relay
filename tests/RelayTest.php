<?php

use Conquest\Relay\Facades\Relay;
use Conquest\Relay\Http\Resources\LangResource;

it('can test', function () {
    // dd(config('relay'));
    // dd(Relay::retrieveTranslations());
    expect(true)->toBeTrue();
});
